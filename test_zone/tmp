#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void)
{
	int argc = 0, index = 0;
	char **argv = NULL;
	char *cmd = NULL, *cmd_dup = NULL;
	char *token, *delim = " \n";
	size_t cmd_buf_size = 0;

	while (1)
	{
		printf("cisfun´⁠◔⁠‿⁠ゝ⁠◔⁠`⁠)⁠━⁠☞ ");
		if (getline(&cmd, &cmd_buf_size, stdin) == -1)
		{
			free(cmd);
			break;
		}

		free(cmd);
	}
	return (0);
}
